{% extends 'base.html.twig' %}

{% block title %}Our Restaurants{% endblock %}

{% block body %}
 
 <div class="col-lg-12">

        <div class="card mb-3">
        <h3 class="card-header">{{ restaurant.name }}</h3>
            <div class="card-body">
                <h5 class="card-title">{{ restaurant.name }} </h5>
                <h6 class="card-subtitle text-muted">
                <a href="{{ path('app_city_show', {'id': restaurant.city.id}) }}">{{ restaurant.city.name }}
                </a> 
                </h6>
            </div>

{{ include('restaurant/_mediafiles.html.twig') }} 

        <div class="card-body">
            <p class="card-text">A description of this beautiful restaurant.</p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">{{ restaurant.getAverageRating }}  </li>
 
        </ul>

{{ include('restaurant/_review_form.html.twig') }}  
        
        </div>
</div>
 
{{ include('restaurant/_ratings.html.twig') }}  

 
 



       



</div> 
{% endblock %}
