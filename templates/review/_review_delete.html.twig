<form method="post" action="{{ path('app_review_delete', {'id': review_id}) }}" onsubmit="return confirm('Are you sure you want to delete this?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review_id) }}">
    <button class="btn btn-danger">Delete</button>
</form>